"use strict";
function numberStack(number) {
    if (!number) {
        return "数字を記入して下さい。";
    }
    let stack = [];
    return stack.push(number);
}
numberStack(2);
numberStack(3);
numberStack(5);
numberStack(6);
numberStack(7);
